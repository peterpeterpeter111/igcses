import {readFileSync,readdirSync,writeFileSync} from 'node:fs';import assert from 'node:assert/strict';const sources=JSON.parse(readFileSync('research/sources.json','utf8'));const rows=readdirSync('content/notes').filter(x=>x.endsWith('.json')).map(f=>JSON.parse(readFileSync('content/notes/'+f,'utf8')));const checks=[];for(const n of rows){const s=sources.find(s=>s.id===n.sourceId);assert(s);assert.equal(n.complete,false);assert.equal(n.humanReviewed,false);assert(n.sourcePages.every(p=>Number.isInteger(p)&&p>0&&p<=s.pages));assert(n.sections.length>0);assert.equal(new Set(n.sections.map(s=>s.id)).size,n.sections.length);assert(n.sections.every(s=>s.paragraphs.length>0&&s.title&&s.id));checks.push({subjectId:n.subjectId,chapterId:n.chapterId,sections:n.sections.length,sourceId:n.sourceId,sourcePages:n.sourcePages,status:'partial-source-checked',complete:false,humanReviewed:false});}
const report={date:'2026-09-09',scope:'Content shape and provenance references. Educational review is separately recorded; this check does not certify full chapters.',chaptersWithPartialNotes:rows.length,sections:rows.reduce((n,r)=>n+r.sections.length,0),completeChapters:0,fullyCoveredSpecificationPoints:0,checks};writeFileSync('research/reviews/2026-09-09-note-check.json',JSON.stringify(report,null,2)+'\n');console.log(JSON.stringify(report,null,2));
