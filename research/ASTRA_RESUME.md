# Astra resume handoff

This handoff is for the next Astra-capable turn after the lower-model repetitive indexing checkpoint. It does not authorise a new batch by itself.

## Current truth

- Preserve all work through the latest commit and `PROGRESS_CHECKPOINT.md`; do not reset or restart.
- Research design is saved in `research/EXTRACTION_WORKFLOW.md`, `research/LEDGER_COLUMNS.md`, `research/schemas/template.schema.json` and `research/TEMPLATE_CONTRACT.md`.
- One pilot only: 4EB1/01 November 2024. It has 11 tasks indexed and Q5 detailed; it is not a fully processed paper.
- Six specification PDFs are obtained, but full statement/skill audit is incomplete. Complete chapters, notes and syllabus points: zero.
- Existing raw discovery ledger remains 390 links: 188 QP-labelled, 186 schemes and 16 reports. Two QP-labelled English rows are actually `Extract` candidates. These are raw links, not canonical paper totals.
- No active generative templates, live generator/marking route, production D1 data, deployed site or verified live URL exists yet. The D1 schema, session/history routes and quiz state/storage contracts are implemented locally and tested with synthetic fixtures.
- OpenAI is the selected provider for the eventual app, but no key has been created or configured in Sites. The user approved secure creation and `.env.local`; callable key-creation tools were unavailable in the last turn. Retry discovery without requesting a secret in chat.

## Lower-model work to review

The user broadened repetitive work to a cross-subject smoke batch and then asked to pause. The bounded batch is now complete as five obtained, indexed-only pairs for 4HB1, 4BI1, 4CH1, 4PH1 and 4EB1; covers were checked, but task-level matching and whole-paper extraction remain pending. 4MB1 remains a discovery gap. Use deterministic downloads/hashes/page counts and source-based indexing only. Do not promote any pair to `processed`, certify mappings, activate a template or infer missing paper identities.

Check the lower-model batch manifest and ledger diffs first. Verify that every selected pair has an exact paper/scheme match, cover identity, page inventory and task index. Send unresolved identity, extraction or access issues here. Reconcile only after inspecting source pages. Keep raw PDFs in ignored `work/` and public metadata copyright-safe.

## Astra-only queue after lower-model checkpoint

1. Review the indexed-only identifiers, covers, scheme matches, page warnings and optional-question arithmetic.
2. Complete structured extraction for selected papers: every leaf task, subpart, marks, command word, AO/skill, source/stimulus reference, scheme criteria, acceptable alternatives, dependencies and report observations.
3. Decide current-specification applicability and map tasks/chapters to verified statement or editorial English skill IDs. Resolve 4MB1 discovery and English/Mathematics inventories.
4. Design and validate original generative families. Preserve official allocations; validate any custom 2/4/6 adaptation independently. Keep family status provisional until multi-seed and educational checks pass.
5. Resolve the remaining quiz concurrency/retry concerns, then integrate the backend-only AI route in the final phase. The subject routes, searchable chapter structure, D1 schema/migrations, coverage UI and quiz session/package contracts already exist locally.
6. Only after build tests and secure configuration: push source to `peterpeterpeter111/igcses`, save a Sites version, deploy and verify the real URL. Never claim coverage or deployment from an expected URL.

## Continue message

“Continue from the latest commit and `PROGRESS_CHECKPOINT.md`; do not restart. The lower-model cross-subject batch is complete as five indexed-only pairs; review its manifest and cover-review overlay first. Treat those records as repetitive indexing candidates, not processed papers. Use Astra for all identity conflicts, syllabus/amendment interpretation, detailed task extraction, template families, validation and architecture. Keep question generation and examiner-style marking until the final pre-deployment phase, using original questions grounded in the validated past-paper/template database. Keep all counts and deployment status honest.”
