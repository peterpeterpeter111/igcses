# Checkpoint — stable chapter path and usage pause

Date: 9 September 2026

## Saved state

The site remains on the pre-opened-book-transition visual state from commit `8c0f505`. This checkpoint adds a focused path layout fix without changing subject data, notes, syllabus ledgers, paper records, quiz contracts or deployment configuration.

## Visual fix completed

- Disabled the moving dash animation that caused the chapter route to flare while scrolling.
- Reduced the route and progress-line shadow to a restrained glow.
- Moved the route into a dedicated left gutter so it cannot sit beneath chapter labels.
- Raised chapter stops and cards above the decorative route as a second protection against text overlap.
- Applied the same gutter separation on narrow screens.

The edited file is `app/theme.css`.

## Verification

- `npm run typecheck` passed.
- `npm run build` passed.
- The application test run reached 31 passing tests with 0 failures; `tests/quiz-client.test.ts` remained pending for about 48 seconds and was cancelled so the run could be stopped. This is an existing runtime-hang issue, unrelated to the CSS change.
- Working tree was clean before this checkpoint change; the CSS and this checkpoint are ready to commit together.

## Usage gate

The Codex primary window is at 100% used, with no reset credits consumed. The gpt-reserve window is available for simpler work, but no further repetitive project processing was started in this turn. Astra should review the visual result before any broader design or curriculum work resumes.

## Resume instruction

Read `PROGRESS_CHECKPOINT.md`, `research/checkpoints/2026-09-09-PATH-FIX.md`, and `CONTINUE_ASTRA.md`. Inspect the chapter path at `/subjects/physics` and `/subjects/human-biology` after starting the local preview. If the path is visually stable, continue from the existing curriculum and paper ledgers. Keep the honest totals unchanged: six partial note documents, six obtained paper/scheme pairs, zero fully processed papers, and zero active templates. Do not reset or discard history.
